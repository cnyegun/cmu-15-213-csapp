;grow the stack for 32 bytes
;rbx = rdi
;rax = fs0x28
;push fs0x28 to the stack
;clears rax
;call string length on rdi
;check it with 6
;if equal to 6 jump to 112
;else bomb

;move 1 byte from rbx(rdi) to ecx
;move 

















;clears rax
;jump to 41
